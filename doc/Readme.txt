Moluptest.exe takes 4 parameters (model file, input, output, #samples) that are required, 
the rest are optional and left to default.

Camo.bat has hard coded sample size of 25

At runtime the exe will prompt user to specify ranges for 
x vars, sample ranges, and PC ranges. Enter  0 to skip (zero not letter o)

prediction results are saved in output.txt